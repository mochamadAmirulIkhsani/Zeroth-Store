{
  "version": 2,
  "builds": [
    { "src": "server.mjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.mjs" },
    { "src": "/(.*)", "dest": "/dist/index.html" }
  ]
}